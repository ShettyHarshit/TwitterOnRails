<div>
<% @posts.each do |post| %>
    <p>
    <tr>
      <h1><td><%= post.user.email %></td></h1>
      <h2><td><%= post.body %></td></h2>
      <h3><td><%= post.created_at %></td></h3>
    </tr>
  <% end %>
  </p>
</div>

<%= link_to 'New Post', new_post_path(@user) %>